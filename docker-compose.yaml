services:
  node:
    image: codebyamrit/student-tracker:stable
    container_name: school-node
    ports:
      - "8080:5000"
    env_file: .env
    environment:
      - NODE_ENV=production
    restart: always
