version: "3.9"

services:
  node:
    image: codebyamrit/student-tracker
    container_name: school-node
    ports:
      - "8080:5000"
    env_file: .env
    environment:
      - NODE_ENV=production
    restart: always
